import { MessageOwnerDirective } from './message-owner.directive';

describe('MessageOwnerDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageOwnerDirective();
    expect(directive).toBeTruthy();
  });
});
